# Only for authoring migrations by hand (`uv run alembic revision -m "..."`).
# The app applies them itself on startup through core.store.db.init_db, against
# DATABASE_URL or the local SQLite file.
[alembic]
script_location = %(here)s/core/store/migrations
prepend_sys_path = .
